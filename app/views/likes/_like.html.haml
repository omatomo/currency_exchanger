- if logged_in?
  - if propose.like_user(current_user.id)
    = render 'likes/destroyLike', propose: propose
  - else
    -# = button_to propose_likes_path(propose), id: "like-button", remote: true do
    -#   = image_tag("1494187707_star.svg")
    = render 'likes/makeLike', propose: propose
- else
  = link_to propose_like_path(id: 1, propose_id: propose.id), method: :delete, id: "like-button", style: "text-decoration: none" do
    .fa.fa-star-o.fa-2x{"aria-hidden" => "true", style: "color: yellow"}
  - if propose.likes_count == nil
    %span.like_count
      0
  - else
    %span.like_count
      = propose.likes_count
    件
