%span
  = link_to propose_likes_path(propose), method: :post, id: "like-button", style: "text-decoration: none" do
    .fa.fa-star-o.fa-2x{"aria-hidden" => "true", style: "color: yellow"}
- if propose.likes_count == nil
  %span.like_count
    0
- else
  %span.like_count
    = propose.likes_count
件
